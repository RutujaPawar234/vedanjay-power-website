/* ============================================================
   Hero/section scene visuals are now provided by real photographs
   (public/images/**). The previous drawn/animated SVG scenes have
   been retired per design direction — only real imagery is shown.
   This component renders nothing; containers keep a plain brand
   background behind the photo as a graceful fallback.
   ============================================================ */
export default function HeroScene() {
  return null;
}
